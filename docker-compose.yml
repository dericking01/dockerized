services:
  msisdn-processor:
    build: .
    volumes:
      - ./files:/app/files
    env_file:
      - .env
    working_dir: /app/src
    command: ["python", "extract_inactive_subscribers_60days.py"]
